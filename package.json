{
  "name": "rice-mill-erp",
  "version": "1.0.0",
  "description": "Full Stack RiceMill Pro ERP",
  "main": "server/server.js",
  "scripts": {
    "build": "npm install && cd client && npm install && npm run build && cd ../server && npm install",
    "start": "node server/server.js",
    "dev": "node server/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  }
}